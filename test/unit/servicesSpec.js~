'use strict';

/* jasmine specs for services go here */

describe('foodService', function() {
  beforeEach(module('nutritionTrackerApp'));

  describe('findFoods', function() {
    it('should return all foods without filter', inject(function(foodService) {
		expect(foodService.findFoods().length).toEqual(3);
    }));
  });
});

